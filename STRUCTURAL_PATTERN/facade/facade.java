package facade;

public class facade {
    public static void main(String[] args) {
        // unify higher-level interfaces for Client to use with ease
        // reduce external complexity and allow interal, like APIs
        // allow DIP and SRP, but somewhat violates OCP (not to Clients)
        // allow obliviousness, as Clients only aware of given operations
    }
}
